(function(window, $, undefined){
    //TODO: namespace instead of window
    var config = {
        search_url: '//localhost:2368/assets/js/faker.js'
    };  
    
    window._config = config;
    return _config;
})(window, jQuery, undefined)
